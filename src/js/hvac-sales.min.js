(function () {

    "use strict"
    var app = angular.module("app", ['ngAnimate']);

    app.controller("MyController", ["$scope", function ($scope) {

        var vm = this;

        vm.salesItem = {};

}]);

}());

/*$(document).ready(function () {
    $("#fileIndoor").bind('change', function () {

        //get the file size and file type from file input field
        var fsize = $('#fileIndoor')[0].files[0].size;
        var ftype = $('#fileIndoor')[0].files[0].type;
        var fname = $('#fileIndoor')[0].files[0].name;

        if (fsize > 4194304) {
            alert("file size can't be more than 4 MB");
            return false;
        }

        switch (ftype) {
        case 'image/png':
        case 'image/gif':
        case 'image/jpeg':
        case 'image/pjpeg':
            alert("Acceptable image file!");
            break;
        default:
            alert('Unsupported File!');
        }
    });
});*/